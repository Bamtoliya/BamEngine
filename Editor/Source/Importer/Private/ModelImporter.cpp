#pragma once

#include "Model.h"
#include "ModelImporter.h"

BEGIN(Editor)
EResult ModelImporter::Import(const filesystem::path& filePath)
{
	// Implementation for importing a model
	return EResult::Success;
}
END